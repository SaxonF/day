- provide(:body, "welcome") 
%header.page-header
  %h1 
    Hi Saxon, 
    %span Welcome to #{@today.strftime("%A")}
%section.day-setup
  .container
    %ul.tasks.epsilon
      = render @tasks
    = render '/shared/task_form'
    = link_to "Start Your Day", start_day_user_path, method: :patch, class: "button"