= form_for [@user, @task], html: {class: "task-form"} do |f|
  = render 'shared/error_messages', object: f.object
  .field
    = f.text_field :title, placeholder: "Compose new task..."
  .hide
    = f.submit "Create", class: "btn btn-large btn-primary"